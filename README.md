# RAD EDOPro

Build repository for the ARC-V RE:PLAY Real Action Duel EDOPro fork.

This repository contains only the RAD overlay, runtime-only custom Action Card assets, and the Windows build workflow. The workflow checks out pinned upstream EDOPro/ygopro-core revisions before applying the overlay.
